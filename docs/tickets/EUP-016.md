# EUP-016 — Docker + Cloudflare Tunnel (PC)

| Field | Value |
|-------|-------|
| **Status** | Done |
| **Phase** | MVP (Phase 1) |
| **Labels** | `mvp`, `infra` |

## Summary

Production-like Docker Compose on Windows PC, exposed publicly via Cloudflare Tunnel at `https://eurovision.grymare.com`. LAN access documented as fallback only.

Absorbs planned Phase 3 items **EUP-032** (Cloudflare Tunnel) and **EUP-033** (DNS subdomain) for this subdomain.

## Acceptance criteria

- [x] `docker compose up --build` serves app on port 3000
- [x] SQLite data persists in named volume
- [x] Deploy guide: [docs/deploy/cloudflare-tunnel.md](../deploy/cloudflare-tunnel.md)
- [x] `cloudflared` config example at [deploy/cloudflared/config.yml.example](../../deploy/cloudflared/config.yml.example)
- [x] README updated — Cloudflare primary, LAN fallback
- [x] Secure session cookies in production (`src/lib/auth/cookies.ts`)
- [x] `.env.example` documents production variables
- [ ] Manual: tunnel + DNS configured in Cloudflare dashboard (see deploy guide)

## Dependencies

- EUP-015 (feature-complete MVP)

## Key decisions

- **Primary hosting:** Cloudflare Tunnel from PC, not LAN-first
- **Pi later:** Same Docker + tunnel pattern (EUP-030)
- **Caddy / port forwarding (EUP-031):** Not required when using tunnel
