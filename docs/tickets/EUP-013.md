# EUP-013 — Ceremony state machine v2 + host API

| Field | Value |
|-------|-------|
| **Status** | Done |
| **Phase** | MVP (Phase 1) |
| **Labels** | `mvp`, `feature` |

## Summary

Replace v1 presentation state with v2: shuffled jury order, running totals, phased reveal flow. Host-only GET/PATCH `/api/parties/[code]/presentation`.

## Acceptance criteria

- [x] v2 state in `revealOrderJson` (juryIndex, phase, runningTotals)
- [x] Shuffle voted jurors on presentation start
- [x] Host-only API with open / reveal / commit / next_jury / finish actions
- [x] Current jury vote data only during reveal phases
- [x] Dev scoreboard fixture compatible
