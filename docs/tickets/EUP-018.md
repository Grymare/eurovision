# EUP-018 — Host-only presentation page

| Field | Value |
|-------|-------|
| **Status** | Done |
| **Phase** | MVP (Phase 1) |
| **Labels** | `mvp`, `feature` |

## Summary

Separate host-only presentation route at `/party/[code]/presentation`. Lobby links here after voting closes; guests see a waiting message during `presenting` and final results when `finished`.

## Acceptance criteria

- [x] Host-only route with guest redirect
- [x] “Open presentation” from lobby when `voting_closed`
- [x] Guest “Presentation in progress” message (no partial scores)
- [x] Guest final scoreboard when party is `finished`
- [x] Removed inline ceremony from lobby
